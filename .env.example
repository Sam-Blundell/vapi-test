# Server
PORT=3000

# Database (SQLite)
# Use ':memory:' for ephemeral runs/tests, or a file path for dev
DB_FILE=./data.db

# Auth (optional)
# If set, require this key via 'x-api-key' or 'Authorization: Bearer <API_KEY>'
# API_KEY=changeme

# CORS (optional)
# Comma-separated list of allowed origins. If empty, allow all (dev convenience).
# Include your ngrok URL for browser calls.
# CORS_ORIGINS=http://localhost:5173,https://logical-jaybird-newly.ngrok-free.app

# Ngrok (optional)
# NGROK_DOMAIN=logical-jaybird-newly.ngrok-free.app
